#ifndef MIDNITE_TYPES
#define MIDNITE_TYPES

typedef unsigned char u8;       // 1 unsigned byte, 8 bits
typedef unsigned short u16;     // 2 unsigned bytes, 16 bits
typedef unsigned int u32;       // 4 unsigned bytes, 32 bits
typedef unsigned long int u64;   // 8 unsigned bytes, 64 bits

typedef char int8;              // 1 byte, 8 bits
typedef short int16;            // 2 bytes, 16 bits
typedef int int32;              // 4 bytes, 32 bits
typedef long int int64;         // 8 bytes, 64 bits

typedef const char* string;

#endif